# رسائل الأخطاء - Error Messages

هذا الملف يوثق نظام رسائل الأخطاء الموحد في التطبيق.

## 📋 نظرة عامة

تم توحيد جميع رسائل الأخطاء في التطبيق لتكون:
- **بالعربية** لتجربة مستخدم أفضل
- **موحدة** عبر جميع أجزاء التطبيق
- **واضحة ومفهومة** للمستخدم النهائي
- **سهلة الصيانة** من مكان واحد

## 🎯 أنواع الأخطاء

### 1. أخطاء الشبكة (Network Errors)
```typescript
NETWORK_ERROR: 'خطأ في الاتصال بالإنترنت. يرجى التحقق من اتصالك والمحاولة مرة أخرى'
```
- تظهر عند فشل الاتصال بالإنترنت
- تظهر عند عدم الوصول للخادم

### 2. أخطاء المصادقة (Authentication Errors)
```typescript
UNAUTHORIZED: 'انتهت صلاحية الجلسة. يرجى تسجيل الدخول مرة أخرى'
INVALID_CREDENTIALS: 'اسم المستخدم أو كلمة المرور غير صحيحة'
SESSION_EXPIRED: 'انتهت صلاحية الجلسة. يرجى تسجيل الدخول مرة أخرى'
```

### 3. أخطاء الصلاحيات (Permission Errors)
```typescript
FORBIDDEN: 'ليس لديك صلاحية للقيام بهذا الإجراء'
ACCESS_DENIED: 'تم رفض الوصول. ليس لديك الصلاحيات المطلوبة'
```

### 4. أخطاء الموارد (Resource Errors)
```typescript
NOT_FOUND: 'المورد المطلوب غير موجود'
RESOURCE_NOT_FOUND: 'لم يتم العثور على البيانات المطلوبة'
```

### 5. أخطاء التحقق (Validation Errors)
```typescript
VALIDATION_ERROR: 'خطأ في التحقق من البيانات. يرجى مراجعة المدخلات'
INVALID_INPUT: 'البيانات المدخلة غير صحيحة'
REQUIRED_FIELD: 'هذا الحقل مطلوب'
```

### 6. أخطاء الخادم (Server Errors)
```typescript
SERVER_ERROR: 'خطأ في الخادم. يرجى المحاولة لاحقاً'
INTERNAL_ERROR: 'حدث خطأ داخلي. يرجى المحاولة مرة أخرى'
SERVICE_UNAVAILABLE: 'الخدمة غير متاحة حالياً. يرجى المحاولة لاحقاً'
```

## 🔧 كيفية الاستخدام

### في API Client
```typescript
import { ERROR_MESSAGES } from '@/shared/constants/errorMessages';

// استخدام مباشر
throw new ApiError(
  ERROR_MESSAGES.NETWORK_ERROR,
  'NETWORK_ERROR',
  0
);
```

### في Thunks
```typescript
import { formatError } from '@/shared/constants/errorMessages';

try {
  // ... code
} catch (error: unknown) {
  const errorMessage = formatError(error);
  return rejectWithValue(errorMessage);
}
```

### في Components
```typescript
import { ERROR_MESSAGES } from '@/shared/constants';

// عرض رسالة خطأ
toast.error(ERROR_MESSAGES.NETWORK_ERROR);
```

## 📦 الدوال المساعدة

### `getErrorMessage(code?: string): string`
تحصل على رسالة الخطأ بناءً على الكود:
```typescript
const message = getErrorMessage('NETWORK_ERROR');
// 'خطأ في الاتصال بالإنترنت. يرجى التحقق من اتصالك والمحاولة مرة أخرى'
```

### `formatError(error: unknown): string`
تحول أي نوع خطأ إلى رسالة نصية:
```typescript
const message = formatError(error);
// يعيد رسالة الخطأ بالعربية
```

## 🎨 معالجة الأخطاء في API Client

تم تحديث `apiClient.ts` ليتعامل مع الأخطاء بشكل موحد:

1. **أخطاء الشبكة**: يتم اكتشافها تلقائياً وعرض رسالة مناسبة
2. **أخطاء HTTP**: كل status code له رسالة خطأ واضحة بالعربية
3. **أخطاء غير متوقعة**: يتم التعامل معها وعرض رسالة عامة

## ✅ أفضل الممارسات

1. **استخدم الثوابت دائماً**: لا تكتب رسائل الأخطاء مباشرة في الكود
   ```typescript
   // ❌ خطأ
   throw new Error('خطأ في الشبكة');
   
   // ✅ صحيح
   throw new Error(ERROR_MESSAGES.NETWORK_ERROR);
   ```

2. **استخدم `formatError` في catch blocks**:
   ```typescript
   try {
     // code
   } catch (error) {
     const message = formatError(error);
     toast.error(message);
   }
   ```

3. **لا تعرض أخطاء تقنية للمستخدم**:
   ```typescript
   // ❌ خطأ
   toast.error(error.stack);
   
   // ✅ صحيح
   toast.error(formatError(error));
   ```

## 🔄 إضافة رسائل خطأ جديدة

لإضافة رسالة خطأ جديدة:

1. افتح `src/shared/constants/errorMessages.ts`
2. أضف الرسالة في `ERROR_MESSAGES`:
   ```typescript
   export const ERROR_MESSAGES = {
     // ... existing messages
     NEW_ERROR: 'رسالة الخطأ الجديدة بالعربية',
   } as const;
   ```
3. استخدمها في الكود:
   ```typescript
   throw new ApiError(ERROR_MESSAGES.NEW_ERROR, 'NEW_ERROR');
   ```

## 📊 تغطية الأخطاء

الملفات التي تم تحديثها لاستخدام رسائل الأخطاء الموحدة:

- ✅ `src/shared/api/apiClient.ts`
- ✅ `src/features/auth/api/authApi.ts`
- ✅ `src/features/auth/store/authThunks.ts`
- ✅ `src/features/auth/store/authSlice.ts`

## 🐛 استكشاف الأخطاء

### المشكلة: رسالة الخطأ بالإنجليزية
**الحل**: تأكد من استيراد `ERROR_MESSAGES` واستخدامها بدلاً من النصوص المباشرة

### المشكلة: "Network error" بالإنجليزية
**الحل**: تم حلها! الآن جميع أخطاء الشبكة تظهر بالعربية تلقائياً

### المشكلة: رسالة خطأ غير واضحة
**الحل**: استخدم `formatError()` لتحويل الخطأ إلى رسالة واضحة
